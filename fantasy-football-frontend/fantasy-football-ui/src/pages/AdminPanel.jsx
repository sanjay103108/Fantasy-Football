const AdminPanel = () => {
    return (
      <div>
        <h2>Admin Panel</h2>
        <p>Only admins can access this section to create and manage contests.</p>
        {/* Add contest creation form, participation logs, etc. */}
      </div>
    );
  };
  
  export default AdminPanel;
  