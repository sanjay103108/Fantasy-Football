import './Topbar.css';

const Topbar = () => {
  return (
    <div className="topbar">
      <span>Fantasy Dashboard</span>
      <button className="logout-btn">Sign Out</button>
    </div>
  );
};

export default Topbar;
